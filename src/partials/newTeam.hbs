<div class="row mx-0">
  {{#each teamMembers}}
    {{#each @root.teamMembers}}
      {{#ifEquals this.name ../this}}
        <div class="col-12 col-md-5 col-lg-3 mb-4">
          {{> newTeamMember }}
        </div>
      {{/ifEquals}}
    {{/each}}
  {{/each}}
</div>
