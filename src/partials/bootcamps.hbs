<!--bootcamps-->
<a id="bootcamps" class="in-page-link"></a>
<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-12 text-center">
                {{#with bootcampsSection}}
                    <h3 class="uppercase mb16">{{{heading}}}</h3>
                    <p class="lead mb64">{{{content}}}</p>
                {{/with}}
            </div>
        </div>

        <div class="row">
            {{#each bootcampsList}}
                {{> course}}
            {{/each}}
        </div>
    </div>
</section>
<!--endbootcamps-->
