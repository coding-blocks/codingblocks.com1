<!--inject:script:js-->
<!--endinject-->
<script>
    $(function () {
        $('.flip-card').flip({
            axis: 'y',
            trigger: 'hover'
        }).waypoint(function (direction) {
            $(this.element).flip(direction == 'down');
        }, {
            offset: '40%'
        })
    })
</script>
